{
    "enabled": {
        "description": "是否启用LLM指令执行器",
        "type": "bool",
        "default": true,
        "hint": "启用后，LLM可以通过execute_command工具代理执行Bot指令"
    },
    "whitelist": {
        "description": "允许执行的指令白名单",
        "type": "list",
        "default": [],
        "hint": "留空表示允许所有指令。填写指令名（不含前缀），如：[\"钓鱼\", \"签到\", \"背包\"]"
    },
    "blacklist": {
        "description": "禁止执行的指令黑名单",
        "type": "list",
        "default": [],
        "hint": "在此列表中的指令将被禁止执行。填写指令名（不含前缀），如：[\"转账\", \"购买\"]"
    },
    "allow_admin_commands": {
        "description": "是否允许执行管理员指令",
        "type": "bool",
        "default": false,
        "hint": "警告：启用此选项可能存在安全风险！建议保持关闭"
    },
    "admin_users": {
        "description": "管理员用户列表，这些用户可以通过 LLM 执行管理员指令（填写用户ID）",
        "type": "list",
        "default": [],
        "hint": "在此列表中的用户可以执行管理员指令，无需开启 allow_admin_commands"
    },
    "bot_user_id": {
        "description": "Bot自己的用户ID",
        "type": "string",
        "default": "bot_self",
        "hint": "当使用as_bot=true时，Bot会以此ID作为自己的身份执行指令。可以设置为Bot的QQ号或其他唯一标识"
    },
    "enable_forward": {
        "description": "是否启用合并转发功能",
        "type": "bool",
        "default": true,
        "hint": "启用后，当指令输出的文本超过阈值时，将自动使用合并转发形式发送"
    },
    "forward_threshold": {
        "description": "合并转发的字数阈值",
        "type": "int",
        "default": 1500,
        "hint": "当指令输出的文本字数超过此值时，将使用合并转发。建议值：1000-2000"
    },
    "at_position_mode": {
        "description": "@用户默认插入位置",
        "type": "string",
        "default": "before_args",
        "hint": "控制未使用 @0 占位符时，@用户默认放在参数前还是参数后。before_args 表示 /命令 @用户 参数，after_args 表示 /命令 参数 @用户"
    },
    "command_at_position_map": {
        "description": "按指令覆盖 @用户插入位置",
        "type": "object",
        "items": {
            "禁言": {
                "type": "string",
                "default": "after_args",
                "hint": "示例：禁言通常可设为 after_args，表示 /禁言 10秒 @用户"
            },
            "转账": {
                "type": "string",
                "default": "before_args",
                "hint": "示例：转账通常可设为 before_args，表示 /转账 @用户 1000"
            }
        },
        "default": {},
        "hint": "按指令名分别指定 @ 位置策略。可选值：before_args 或 after_args。未指定的指令会回退到 at_position_mode"
    }
}